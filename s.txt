#hello

$toprod := 4;
while(1)
{
	print("This move number", ?turn());
	buy(2, ?raw_price());
	sell(?production(?my_id()), ?production_price());
	$toprod:= 2;
	if (?raw(?my_id())<=$toprod)
		$toprod :=?raw(?my_id());
	prod($toprod);
	end_turn()
}
if ($toprod[2])
{
	print("hello")
	prod(2);
}
end_turn();
