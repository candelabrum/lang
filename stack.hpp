#include "rpn_list.hpp"
/*
class RPNStack : RPNList
{
	RPNElem *head;
public:
	bool IsEmpty() const { return is_empty(); }
	void Push(RPNElem* elem) { add_node(a_data); }
	T& Pop() { return get_data(0); } 
	void Print_stack() const { print(); }
	void Delete_Stack() { Delete(); }
};

bool RPNStack::IsEmpty() const
{
	return head;
}

void RPNStack::Push(RPNElem* elem)
{
	node *new_node = new node(a_data); 

	if (!head)
		end = new_node;
	new_node->next = head;
	head = new_node;


class Stack<*RPNElem> : public RPNElem
{
	void print() const { printf("stack"); } 
	virtual void Evaluate(RPNItem **stack,
								RPNItem **cur_cmd) const
	{
		printf("It's Stack\n");
	}
	virtual RPNElem* Convert2RPNElem(lexeme *l) const
	{
		printf("You are using convert, but it's not convert\n");
	}
};
*/
